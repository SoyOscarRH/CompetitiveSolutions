#include <algorithm>
#include <iostream>
#include <set>
#include <string>
#include <vector>

using namespace std;

constexpr auto endline = '\n';

auto solve() {
  int num_mappings;
  cin >> num_mappings;

  auto num_of = map<string, int>();
  auto string_of = map<int, string>();


  for (auto i = 0; i < num_mappings; ++i) {

  }
}

auto main() -> int {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);

  solve();

  return 0;
}