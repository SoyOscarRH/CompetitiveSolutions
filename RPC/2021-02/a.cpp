#include <algorithm>
#include <iostream>
#include <string>
#include <vector>

using namespace std;

constexpr auto endline = '\n';

auto solve() -> int { return 0; }

auto main() -> int {
  ios::sync_with_stdio(false);

  auto tests = 10;
  cin >> tests;

  while (tests--) { solve(); }

  return 0;
}