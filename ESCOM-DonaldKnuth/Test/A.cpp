#include <algorithm>
#include <cmath>
#include <iomanip>
#include <iostream>
#include <utility>
#include <vector>

using namespace std;

auto main() -> int {
  ios::sync_with_stdio(false);
  int64_t a, b;
  cin >> a >> b;

  cout << a + b << endl;

  return 0;
}